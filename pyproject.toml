[project]
name = "anysearch"
version = "0.0.1"
description = "Real-time web search, vertical domain search, and web page extraction for Dify"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.10.2",
    "requests>=2.32.0",
]

# uv run black . -C -l 100 && uv run ruff check --fix
[dependency-groups]
dev = []
